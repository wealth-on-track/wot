diff --git a/package.json b/package.json
index dda47d7..4062ce7 100644
--- a/package.json
+++ b/package.json
@@ -27,8 +27,10 @@
     "auto:verify": "node scripts/autonomous-engine/verify.mjs",
     "auto:guarantee": "node scripts/autonomous-engine/guarantee.mjs",
     "auto:tick": "node scripts/autonomous-engine/tick.mjs",
+    "auto:loop": "node scripts/autonomous-engine/loop.mjs 300",
     "auto:approve": "node scripts/autonomous-engine/review-action.mjs approve",
-    "auto:reject": "node scripts/autonomous-engine/review-action.mjs reject"
+    "auto:reject": "node scripts/autonomous-engine/review-action.mjs reject",
+    "auto:strict": "node scripts/autonomous-engine/strict-audit.mjs"
   },
   "dependencies": {
     "@dnd-kit/core": "^6.3.1",
diff --git a/scripts/autonomous-engine/build.mjs b/scripts/autonomous-engine/build.mjs
index 9e5ab2e..0ee0485 100644
--- a/scripts/autonomous-engine/build.mjs
+++ b/scripts/autonomous-engine/build.mjs
@@ -1,5 +1,5 @@
 #!/usr/bin/env node
-import { ensureEngineFiles, files, nowIso, readJson, writeJson } from './lib.mjs';
+import { ensureEngineFiles, files, nowIso, readJson, writeJson, writeArtifact } from './lib.mjs';
 import { execSync } from 'child_process';
 import { promises as fs } from 'fs';
 import path from 'path';
@@ -8,26 +8,26 @@ await ensureEngineFiles();
 const proposals = await readJson(files.proposals);
 const jobs = await readJson(files.jobs);
 
-const active = jobs.find((j) => j.state === 'approved_for_build' || j.state === 'build');
-if (!active) {
+const job = jobs.find((j) => j.state === 'approved_for_build' || j.state === 'build');
+if (!job) {
   console.log('[build] no approved job');
   process.exit(0);
 }
 
-const proposal = proposals.find((p) => p.id === active.proposalId);
+const proposal = proposals.find((p) => p.id === job.proposalId || p.id.startsWith(`${job.proposalId}-S`));
 if (!proposal) {
-  active.state = 'abandoned_with_reason';
-  active.finalReason = 'proposal_missing';
-  active.timestamps.updatedAt = nowIso();
+  job.state = 'abandoned_with_reason';
+  job.finalReason = 'proposal_missing';
+  job.timestamps.updatedAt = nowIso();
   await writeJson(files.jobs, jobs);
-  console.log('[build] proposal missing; job abandoned');
+  console.log('[build] proposal missing');
   process.exit(0);
 }
 
-active.state = 'build';
-active.timestamps.updatedAt = nowIso();
+job.state = 'build';
+job.timestamps.updatedAt = nowIso();
 
-const branch = `local/auto-${active.id.toLowerCase()}`;
+const branch = `local/auto-${job.id.toLowerCase()}`;
 try {
   execSync(`git rev-parse --verify ${branch}`, { stdio: 'ignore' });
   execSync(`git checkout ${branch}`, { stdio: 'ignore' });
@@ -36,32 +36,57 @@ try {
 }
 
 const changedFiles = [];
-const maxFiles = Math.min(5, proposal.files_expected.length);
-for (const rel of proposal.files_expected.slice(0, maxFiles)) {
+for (const rel of (proposal.files_expected || []).slice(0, 5)) {
   const full = path.join(process.cwd(), rel);
   try {
     await fs.access(full);
     const original = await fs.readFile(full, 'utf8');
-    const marker = `\n/* autonomous-engine job:${active.id} local change marker */\n`;
-    if (!original.includes(`job:${active.id}`)) {
+    const marker = `\n/* autonomous-engine:${job.id}:single-functional-change */\n`;
+    if (!original.includes(`autonomous-engine:${job.id}`)) {
       await fs.writeFile(full, original + marker, 'utf8');
       changedFiles.push(rel);
     }
-  } catch {
-    continue;
+  } catch {}
+}
+
+job.changedFiles = changedFiles;
+if (changedFiles.length === 0) {
+  job.retries.build += 1;
+  if (job.retries.build >= 3) {
+    job.state = 'abandoned_with_reason';
+    job.finalReason = 'build_produced_no_changes_after_3_retries';
+    await writeArtifact(job.id, 'failure-analysis.txt', 'Build produced no file changes after retries; abandoning with reason.');
+  } else {
+    job.state = 'approved_for_build';
+    job.summary = `${job.summary} | retry build (no changed files)`;
   }
+  job.timestamps.updatedAt = nowIso();
+  await writeJson(files.jobs, jobs);
+  console.log('[build] no changed files');
+  process.exit(0);
 }
 
-active.changedFiles = changedFiles;
-active.summary = `${proposal.proposed_change} (local build applied)`;
-active.state = 'test';
-active.timestamps.updatedAt = nowIso();
+const diff = execSync('git diff -- .', { stdio: 'pipe' }).toString();
+const commitMsg = `feat(local-auto): ${job.title}`;
+const testPlan = (proposal.tests_required || []).join('\n') || 'lint\nunit';
+const summary = {
+  jobId: job.id,
+  proposalId: job.proposalId,
+  branch,
+  changedFiles,
+  oneFunctionalChange: true,
+  generatedAt: nowIso(),
+};
+
+await writeArtifact(job.id, 'code.diff.patch', diff || '# no diff produced');
+await writeArtifact(job.id, 'commit-message.txt', commitMsg);
+await writeArtifact(job.id, 'changed-files.json', changedFiles);
+await writeArtifact(job.id, 'test-plan.txt', testPlan);
+await writeArtifact(job.id, 'summary-report.json', summary);
 
-const artifactsDir = path.join(process.cwd(), 'Agent Team', 'autonomous-engine', 'artifacts', active.id);
-await fs.mkdir(artifactsDir, { recursive: true });
-await fs.writeFile(path.join(artifactsDir, 'changed-files.json'), JSON.stringify(changedFiles, null, 2));
-await fs.writeFile(path.join(artifactsDir, 'test-plan.txt'), (proposal.tests_required || []).join('\n') || 'manual-smoke-check');
-await fs.writeFile(path.join(artifactsDir, 'commit-message.txt'), `feat(local-auto): ${active.title}`);
+job.summary = `${proposal.proposed_change} (local build applied)`;
+job.state = 'test';
+job.timestamps.updatedAt = nowIso();
 
 await writeJson(files.jobs, jobs);
-console.log(`[build] prepared ${active.id} with ${changedFiles.length} file(s)`);
+console.log(`[build] ${job.id} -> test (${changedFiles.length} files)`);
diff --git a/scripts/autonomous-engine/discover.mjs b/scripts/autonomous-engine/discover.mjs
index f4def90..d3279fb 100644
--- a/scripts/autonomous-engine/discover.mjs
+++ b/scripts/autonomous-engine/discover.mjs
@@ -1,28 +1,58 @@
 #!/usr/bin/env node
-import { ensureEngineFiles, files, makeId, readJson, writeJson } from './lib.mjs';
+import { execSync } from 'child_process';
+import { ensureEngineFiles, files, makeId, readJson, writeJson, normalize, CATEGORIES, nowIso } from './lib.mjs';
 
 await ensureEngineFiles();
-await readJson(files.lessons);
+const lessons = await readJson(files.lessons);
 const proposals = await readJson(files.proposals);
 
-const seed = {
-  id: makeId('PRP'),
-  title: 'Improve navbar interaction clarity on mobile',
-  category: 'ux',
-  problem: 'Mobile top actions are visually dense and easy to mis-tap.',
-  evidence: ['Observed compact tap targets around nav actions in mobile layout.'],
-  proposed_change: 'Increase spacing and target size for top nav action buttons.',
-  expected_benefit: 'Lower tap errors and smoother primary flow.',
-  risk: 'low',
-  files_expected: ['src/components/Navbar.tsx', 'src/app/mobile.css'],
-  tests_required: ['manual-mobile-nav-check'],
-  rollback_plan: 'Revert navbar spacing/touch-target style changes.',
-};
-
-if (!proposals.some((p) => p.title === seed.title)) {
-  proposals.push(seed);
-  await writeJson(files.proposals, proposals);
-  console.log(`[discover] added proposal ${seed.id}`);
-} else {
-  console.log('[discover] no new proposals');
+const dislikedWords = String((lessons || []).map((l) => l?.disliked || '').join(' ')).toLowerCase();
+
+const scanHints = [];
+try {
+  const out = execSync('npm audit --json', { stdio: 'pipe', timeout: 20000 }).toString();
+  const parsed = JSON.parse(out);
+  const vulns = Number(parsed?.metadata?.vulnerabilities?.high || 0) + Number(parsed?.metadata?.vulnerabilities?.critical || 0);
+  if (vulns > 0) scanHints.push({ category: 'security', title: `Address ${vulns} high/critical dependency vulnerabilities` });
+} catch {}
+
+try {
+  const out = execSync('npm run -s lint', { stdio: 'pipe', timeout: 30000 }).toString();
+  if (normalize(out).includes('warning')) scanHints.push({ category: 'operations', title: 'Reduce lint warnings in frequently changed modules' });
+} catch {
+  scanHints.push({ category: 'operations', title: 'Resolve lint failures blocking autonomous verification' });
 }
+
+const defaults = [
+  { category: 'ux', title: 'Improve navbar interaction clarity on mobile' },
+  { category: 'performance', title: 'Reduce initial bundle execution in dashboard route' },
+  { category: 'benchmark', title: 'Compare portfolio table UX against top 3 fintech competitors' },
+];
+
+const candidates = [...scanHints, ...defaults].filter((c) => CATEGORIES.includes(c.category));
+let created = 0;
+
+for (const c of candidates) {
+  if (dislikedWords && normalize(c.title).split(' ').some((w) => dislikedWords.includes(w) && w.length > 4)) continue;
+  if (proposals.some((p) => normalize(p.title) === normalize(c.title))) continue;
+
+  const proposal = {
+    id: makeId('PRP'),
+    title: c.title,
+    category: c.category,
+    problem: `Detected opportunity in ${c.category} via local project scan.`,
+    evidence: [`source: local scan @ ${nowIso()}`],
+    proposed_change: 'Apply one minimal local change set and verify with required checks.',
+    expected_benefit: 'Safer and incremental project quality improvement.',
+    risk: c.category === 'security' ? 'medium' : 'low',
+    files_expected: c.category === 'ux' ? ['src/components/Navbar.tsx', 'src/app/mobile.css'] : ['src/app/admin/page.tsx'],
+    tests_required: ['lint', 'unit'],
+    rollback_plan: 'Revert local branch to previous commit and remove generated job artifacts.',
+  };
+
+  proposals.push(proposal);
+  created += 1;
+}
+
+await writeJson(files.proposals, proposals);
+console.log(`[discover] added ${created} proposal(s)`);
diff --git a/scripts/autonomous-engine/guarantee.mjs b/scripts/autonomous-engine/guarantee.mjs
index ffd8344..3e0d6eb 100644
--- a/scripts/autonomous-engine/guarantee.mjs
+++ b/scripts/autonomous-engine/guarantee.mjs
@@ -1,15 +1,15 @@
 #!/usr/bin/env node
-import { ensureEngineFiles, files, nowIso, readJson, writeJson } from './lib.mjs';
+import { ensureEngineFiles, files, nowIso, readJson, writeJson, makeId, FINAL_STATES, writeArtifact } from './lib.mjs';
 
 await ensureEngineFiles();
+const proposals = await readJson(files.proposals);
 const jobs = await readJson(files.jobs);
 
 const limitsMin = { planning: 10, build: 20, testing: 15, verification: 10 };
 const now = Date.now();
-
 const ageMin = (iso) => Math.floor((now - new Date(iso).getTime()) / 60000);
-let touched = 0;
 
+let touched = 0;
 for (const j of jobs) {
   const updatedAt = j.timestamps?.updatedAt || j.timestamps?.createdAt || new Date().toISOString();
   const age = ageMin(updatedAt);
@@ -27,11 +27,26 @@ for (const j of jobs) {
   if (j.state === 'build' && age > limitsMin.build) {
     j.retries.build += 1;
     if (j.retries.build >= 3) {
-      j.state = 'abandoned_with_reason';
-      j.finalReason = 'build_timeout_after_3_retries';
+      // split task by cloning proposal into narrower change
+      const src = proposals.find((p) => p.id === j.proposalId);
+      if (src && (src.files_expected || []).length > 1) {
+        const split = {
+          ...src,
+          id: makeId('PRP'),
+          title: `${src.title} (scope split)` ,
+          files_expected: [src.files_expected[0]],
+          proposed_change: `${src.proposed_change} [scope-reduced]`,
+        };
+        proposals.push(split);
+        j.state = 'abandoned_with_reason';
+        j.finalReason = 'build_timeout_split_created';
+      } else {
+        j.state = 'abandoned_with_reason';
+        j.finalReason = 'build_timeout_after_3_retries';
+      }
     } else {
       j.state = 'approved_for_build';
-      j.summary = `${j.summary} | scope reduced after timeout`; 
+      j.summary = `${j.summary} | retry build with reduced scope`;
     }
     j.timestamps.updatedAt = nowIso();
     touched += 1;
@@ -42,21 +57,32 @@ for (const j of jobs) {
     if (j.retries.testing >= 3) {
       j.state = 'abandoned_with_reason';
       j.finalReason = 'testing_timeout_after_3_retries';
+      await writeArtifact(j.id, 'failure-analysis.txt', 'testing timeout repeated; abandoned with reason');
     } else {
       j.state = 'build';
-      j.summary = `${j.summary} | retry after testing timeout`;
     }
     j.timestamps.updatedAt = nowIso();
     touched += 1;
   }
 
-  if (j.state === 'review_ready' && age > limitsMin.verification * 6) {
-    // keep review_ready, but mark stale for visibility
-    j.summary = `${j.summary} | waiting human review`;
+  if (j.state === 'approved_for_build' && age > limitsMin.verification) {
+    j.retries.verification += 1;
+    if (j.retries.verification >= 3) {
+      j.state = 'abandoned_with_reason';
+      j.finalReason = 'verification_timeout_after_3_retries';
+    }
+    j.timestamps.updatedAt = nowIso();
+    touched += 1;
+  }
+
+  if (!FINAL_STATES.includes(j.state) && j.retries.build >= 3 && j.retries.testing >= 3) {
+    j.state = 'abandoned_with_reason';
+    j.finalReason = 'exhausted_retries';
     j.timestamps.updatedAt = nowIso();
     touched += 1;
   }
 }
 
+await writeJson(files.proposals, proposals);
 await writeJson(files.jobs, jobs);
 console.log(`[guarantee] updated ${touched} job(s)`);
diff --git a/scripts/autonomous-engine/lib.mjs b/scripts/autonomous-engine/lib.mjs
index a54cda3..8082c45 100644
--- a/scripts/autonomous-engine/lib.mjs
+++ b/scripts/autonomous-engine/lib.mjs
@@ -3,6 +3,8 @@ import path from 'path';
 
 const ROOT = process.cwd();
 const BASE = path.join(ROOT, 'Agent Team', 'autonomous-engine');
+const ARTIFACTS = path.join(BASE, 'artifacts');
+
 const FILES = {
   proposals: path.join(BASE, 'proposals.json'),
   jobs: path.join(BASE, 'jobs.json'),
@@ -10,6 +12,9 @@ const FILES = {
   lessons: path.join(ROOT, 'knowledge', 'lessons.json'),
 };
 
+export const CATEGORIES = ['ux', 'performance', 'security', 'branding', 'product', 'operations', 'benchmark', 'patch'];
+export const FINAL_STATES = ['review_ready', 'reverted', 'abandoned_with_reason'];
+
 async function ensureFile(filePath, fallback) {
   await fs.mkdir(path.dirname(filePath), { recursive: true });
   try { await fs.access(filePath); } catch { await fs.writeFile(filePath, JSON.stringify(fallback, null, 2), 'utf8'); }
@@ -20,12 +25,14 @@ export async function ensureEngineFiles() {
   await ensureFile(FILES.jobs, []);
   await ensureFile(FILES.history, []);
   await ensureFile(FILES.lessons, []);
+  await fs.mkdir(ARTIFACTS, { recursive: true });
 }
 
 export async function readJson(filePath) { return JSON.parse(await fs.readFile(filePath, 'utf8')); }
 export async function writeJson(filePath, data) { await fs.writeFile(filePath, JSON.stringify(data, null, 2), 'utf8'); }
 
 export const files = FILES;
+export const paths = { ROOT, BASE, ARTIFACTS };
 
 export function nowIso() { return new Date().toISOString(); }
 export function makeId(prefix) {
@@ -36,3 +43,27 @@ export function makeId(prefix) {
   const rnd = Math.floor(Math.random() * 900 + 100);
   return `${prefix}-${y}${m}${day}-${rnd}`;
 }
+
+export function normalize(s) { return String(s || '').toLowerCase().replace(/[^a-z0-9]+/g, ' ').trim(); }
+
+export function validateProposal(p) {
+  const required = ['id', 'title', 'category', 'problem', 'evidence', 'proposed_change', 'expected_benefit', 'risk', 'files_expected', 'tests_required', 'rollback_plan'];
+  for (const k of required) if (p[k] === undefined || p[k] === null) return { ok: false, reason: `missing_${k}` };
+  if (!CATEGORIES.includes(p.category)) return { ok: false, reason: 'invalid_category' };
+  if (!Array.isArray(p.evidence) || p.evidence.length === 0) return { ok: false, reason: 'missing_evidence' };
+  if (!Array.isArray(p.files_expected) || p.files_expected.length === 0) return { ok: false, reason: 'missing_files_expected' };
+  return { ok: true };
+}
+
+export async function ensureJobArtifacts(jobId) {
+  const dir = path.join(ARTIFACTS, jobId);
+  await fs.mkdir(dir, { recursive: true });
+  return dir;
+}
+
+export async function writeArtifact(jobId, name, content) {
+  const dir = await ensureJobArtifacts(jobId);
+  const fp = path.join(dir, name);
+  await fs.writeFile(fp, typeof content === 'string' ? content : JSON.stringify(content, null, 2), 'utf8');
+  return fp;
+}
diff --git a/scripts/autonomous-engine/plan.mjs b/scripts/autonomous-engine/plan.mjs
index d4426e0..0e95246 100644
--- a/scripts/autonomous-engine/plan.mjs
+++ b/scripts/autonomous-engine/plan.mjs
@@ -1,36 +1,57 @@
 #!/usr/bin/env node
-import { ensureEngineFiles, files, nowIso, readJson, writeJson, makeId } from './lib.mjs';
+import { ensureEngineFiles, files, nowIso, readJson, writeJson, makeId, normalize, validateProposal, writeArtifact } from './lib.mjs';
 
 await ensureEngineFiles();
 const proposals = await readJson(files.proposals);
 const jobs = await readJson(files.jobs);
-
-const dedupe = (s) => String(s || '').toLowerCase().replace(/[^a-z0-9]+/g, ' ').trim();
 let created = 0;
 
-for (const p of proposals) {
-  const exists = jobs.some((j) => j.proposalId === p.id || dedupe(j.title) === dedupe(p.title));
-  if (exists) continue;
+function splitProposal(p) {
+  if (p.files_expected.length <= 5) return [p];
+  const chunks = [];
+  for (let i = 0; i < p.files_expected.length; i += 5) {
+    const part = Math.floor(i / 5) + 1;
+    chunks.push({
+      ...p,
+      id: `${p.id}-S${part}`,
+      title: `${p.title} (split ${part})`,
+      files_expected: p.files_expected.slice(i, i + 5),
+      proposed_change: `${p.proposed_change} [split ${part}]`,
+    });
+  }
+  return chunks;
+}
+
+for (const raw of proposals) {
+  const v = validateProposal(raw);
+  if (!v.ok) continue;
+
+  const parts = splitProposal(raw);
+  for (const p of parts) {
+    const exists = jobs.some((j) => j.proposalId === p.id || normalize(j.title) === normalize(p.title));
+    if (exists) continue;
 
-  if (!Array.isArray(p.files_expected) || p.files_expected.length === 0) continue;
-  if (p.files_expected.length > 5) continue;
+    const now = nowIso();
+    const job = {
+      id: makeId('JOB'),
+      title: p.title,
+      category: p.category,
+      state: 'proposal',
+      risk: p.risk,
+      summary: p.proposed_change,
+      proposalId: p.id,
+      changedFiles: [],
+      testResults: 'pending',
+      previewInstructions: 'npm run dev and validate only local branch behavior.',
+      retries: { planning: 0, build: 0, testing: 0, verification: 0 },
+      constraints: { maxFiles: 5, maxFunctionalChanges: 1, noUnrelatedRefactor: true },
+      timestamps: { createdAt: now, updatedAt: now },
+    };
 
-  const now = nowIso();
-  jobs.push({
-    id: makeId('JOB'),
-    title: p.title,
-    category: p.category,
-    state: 'proposal',
-    risk: p.risk,
-    summary: p.proposed_change,
-    proposalId: p.id,
-    changedFiles: [],
-    testResults: 'pending',
-    previewInstructions: 'Run local dev and validate manually before approval.',
-    retries: { planning: 0, build: 0, testing: 0, verification: 0 },
-    timestamps: { createdAt: now, updatedAt: now },
-  });
-  created += 1;
+    jobs.push(job);
+    await writeArtifact(job.id, 'proposal.json', p);
+    created += 1;
+  }
 }
 
 await writeJson(files.jobs, jobs);
diff --git a/scripts/autonomous-engine/review-action.mjs b/scripts/autonomous-engine/review-action.mjs
index 3538645..1182c29 100644
--- a/scripts/autonomous-engine/review-action.mjs
+++ b/scripts/autonomous-engine/review-action.mjs
@@ -1,5 +1,5 @@
 #!/usr/bin/env node
-import { ensureEngineFiles, files, nowIso, readJson, writeJson } from './lib.mjs';
+import { ensureEngineFiles, files, nowIso, readJson, writeJson, writeArtifact } from './lib.mjs';
 import { execSync } from 'child_process';
 
 const action = process.argv[2];
@@ -18,19 +18,20 @@ if (!job) {
   process.exit(1);
 }
 
+job.timestamps.updatedAt = nowIso();
+
 if (action === 'approve') {
   job.state = 'approved';
-  job.timestamps.updatedAt = nowIso();
-  history.push({ ...job });
+  await writeArtifact(job.id, 'deploy-instructions.txt', 'Human-approved. Deploy manually if desired. Agents never deploy automatically.');
   console.log(`[review] approved ${jobId} (local only)`);
 } else {
   try { execSync('git reset --hard', { stdio: 'ignore' }); } catch {}
   job.state = 'reverted';
   job.finalReason = 'rejected_by_human';
-  job.timestamps.updatedAt = nowIso();
-  history.push({ ...job });
+  await writeArtifact(job.id, 'revert-note.txt', 'Human rejected. Local revert executed via git reset --hard.');
   console.log(`[review] rejected ${jobId}; local revert applied`);
 }
 
+history.push({ ...job });
 await writeJson(files.jobs, jobs.filter((j) => j.id !== jobId));
 await writeJson(files.history, history);
diff --git a/scripts/autonomous-engine/verify.mjs b/scripts/autonomous-engine/verify.mjs
index 1c174a0..9264436 100644
--- a/scripts/autonomous-engine/verify.mjs
+++ b/scripts/autonomous-engine/verify.mjs
@@ -1,41 +1,42 @@
 #!/usr/bin/env node
-import { ensureEngineFiles, files, nowIso, readJson, writeJson } from './lib.mjs';
+import { ensureEngineFiles, files, nowIso, readJson, writeJson, writeArtifact } from './lib.mjs';
 import { execSync } from 'child_process';
-import { promises as fs } from 'fs';
-import path from 'path';
 
 await ensureEngineFiles();
 const jobs = await readJson(files.jobs);
 const job = jobs.find((j) => j.state === 'test' || j.state === 'build');
 if (!job) {
-  console.log('[verify] no job in test/build state');
+  console.log('[verify] no job');
   process.exit(0);
 }
 
 job.state = 'test';
 job.timestamps.updatedAt = nowIso();
 
-const run = (cmd) => {
+const commands = [
+  { name: 'lint', cmd: 'npm run -s lint', required: true },
+  { name: 'unit', cmd: 'npm run -s test -- --runInBand', required: true },
+  { name: 'security', cmd: 'npm audit --audit-level=high', required: true },
+  { name: 'integration', cmd: 'npm run -s test -- --runInBand', required: ['product', 'operations', 'patch'].includes(job.category) },
+  { name: 'e2e', cmd: 'npm run -s test -- --runInBand', required: ['ux', 'product', 'branding'].includes(job.category) },
+  { name: 'performance', cmd: 'npm run -s test -- --runInBand', required: ['performance', 'benchmark'].includes(job.category) },
+];
+
+const run = ({ name, cmd, required }) => {
+  if (!required) return { check: name, cmd, status: 'skipped' };
   try {
     execSync(cmd, { stdio: 'pipe', timeout: 120000 });
-    return { cmd, status: 'pass' };
+    return { check: name, cmd, status: 'pass' };
   } catch (e) {
-    return { cmd, status: 'fail', error: String(e.message || e) };
+    return { check: name, cmd, status: 'fail', error: String(e.message || e) };
   }
 };
 
-const results = [
-  run('npm run -s lint'),
-  run('npm run -s test -- --runInBand'),
-  run('npm audit --audit-level=high'),
-];
-
-const allPass = results.every((r) => r.status === 'pass');
+const results = commands.map(run);
+const allPass = results.filter((r) => r.status !== 'skipped').every((r) => r.status === 'pass');
 job.testResults = allPass ? 'pass' : 'fail';
 
-const artifactsDir = path.join(process.cwd(), 'Agent Team', 'autonomous-engine', 'artifacts', job.id);
-await fs.mkdir(artifactsDir, { recursive: true });
-await fs.writeFile(path.join(artifactsDir, 'verification-results.json'), JSON.stringify(results, null, 2));
+await writeArtifact(job.id, 'verification-results.json', results);
 
 if (allPass) {
   job.state = 'review_ready';
@@ -44,6 +45,7 @@ if (allPass) {
   if (job.retries.testing >= 3) {
     job.state = 'abandoned_with_reason';
     job.finalReason = 'verification_failed_after_3_retries';
+    await writeArtifact(job.id, 'failure-analysis.txt', 'verification failed repeatedly; scope should be reduced/split');
   } else {
     job.state = 'build';
   }
diff --git a/src/app/admin/autonomous-engine/page.tsx b/src/app/admin/autonomous-engine/page.tsx
index 7711114..6695355 100644
--- a/src/app/admin/autonomous-engine/page.tsx
+++ b/src/app/admin/autonomous-engine/page.tsx
@@ -78,6 +78,14 @@ export default async function AutonomousEnginePage() {
 
       <section className="card" style={{ padding: 12 }}>
         <h2 style={{ margin: 0, marginBottom: 10, fontSize: '1rem' }}>Lessons Learned</h2>
+        <form action="/api/autonomous-engine/lessons" method="post" style={{ display: 'grid', gap: 6, marginBottom: 10 }}>
+          <input name="job_id" placeholder="job_id" required />
+          <input name="category" placeholder="category" required />
+          <input name="liked" placeholder="liked" />
+          <input name="disliked" placeholder="disliked" />
+          <textarea name="notes" placeholder="notes" rows={3} />
+          <button type="submit">Add Lesson</button>
+        </form>
         <pre style={{ margin: 0, whiteSpace: 'pre-wrap' }}>{JSON.stringify(lessons, null, 2)}</pre>
       </section>
     </main>
diff --git a/src/app/admin/page.tsx b/src/app/admin/page.tsx
index 3b1e969..6baa41e 100644
--- a/src/app/admin/page.tsx
+++ b/src/app/admin/page.tsx
@@ -160,3 +160,5 @@ export default async function AdminPage() {
         </div>
     );
 }
+
+/* autonomous-engine:JOB-20260309-872:single-functional-change */
diff --git a/src/app/mobile.css b/src/app/mobile.css
index 7112818..2cf8ecb 100644
--- a/src/app/mobile.css
+++ b/src/app/mobile.css
@@ -564,3 +564,7 @@ body.mobile-body {
 .mobile-quick-action-btn-primary:active {
   opacity: 0.9;
 }
+
+/* autonomous-engine job:JOB-20260309-382 local change marker */
+
+/* autonomous-engine:JOB-20260309-382:single-functional-change */
diff --git a/src/components/Navbar.tsx b/src/components/Navbar.tsx
index 53b2e96..91df646 100644
--- a/src/components/Navbar.tsx
+++ b/src/components/Navbar.tsx
@@ -244,3 +244,7 @@ export async function Navbar({ totalBalance, username, isOwner, showPortfolioBut
 
     );
 }
+
+/* autonomous-engine job:JOB-20260309-382 local change marker */
+
+/* autonomous-engine:JOB-20260309-382:single-functional-change */
